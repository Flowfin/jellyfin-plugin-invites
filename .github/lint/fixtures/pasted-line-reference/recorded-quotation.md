# A page quoting a line as it read on a day

Checked once, and the quotation below is what the target carried then rather
than a claim about what it carries now. The register names it, so the check
counts it and judges nothing.

    .github/lint/fixtures/pasted-line-reference/target.txt:3:A line a fixture points at.

Nothing else on this page carries a line number.
