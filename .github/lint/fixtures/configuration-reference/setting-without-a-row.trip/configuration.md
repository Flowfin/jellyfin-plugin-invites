# Configuration reference, fixture

One row short. `MaximumUseCount` is in the type beside this file and has no row
here, which is the direction that leaves an operator reading about a setting
that is not the one they are looking at.

| Setting             | What it does                     | Default | Bounds |
| ------------------- | -------------------------------- | ------- | ------ |
| `PublicBaseAddress` | The address links are built from | unset   | a URL  |
