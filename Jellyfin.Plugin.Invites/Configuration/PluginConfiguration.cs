using MediaBrowser.Model.Plugins;

namespace Jellyfin.Plugin.Invites.Configuration;

/// <summary>
/// Plugin configuration.
/// </summary>
/// <remarks>
/// Deliberately carries no settings yet. The class and the configuration page exist so the
/// plumbing is in place, and the settings this plugin actually needs are added later.
/// </remarks>
public class PluginConfiguration : BasePluginConfiguration
{
}
